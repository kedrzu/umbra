{
  "mcpServers": {
    "gmail": {
      "type": "http",
      "url": "http://localhost:${MCP_GMAIL_PORT}/mcp"
    },
    "calendar": {
      "type": "http",
      "url": "http://localhost:${MCP_CALENDAR_PORT}/mcp"
    },
    "todoist": {
      "type": "http",
      "url": "https://ai.todoist.net/mcp"
    },
    "qmd": {
      "command": "${QMD_PATH}",
      "args": ["mcp"],
      "env": {
        "QMD_VAULT_PATH": "${OBSIDIAN_VAULT_PATH}"
      }
    }
  }
}
